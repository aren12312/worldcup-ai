# WorldCup AI Frontend

Next.js web UI for match predictions.

```bash
cp .env.local.example .env.local
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` to your backend (default `http://localhost:8080`).
